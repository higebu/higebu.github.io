language: python
dist: trusty
python:
  - "3.5"
branches:
  only:
    - source
before_install:
  - pip install -U pip
install:
  - pip install -r requirements.txt
  - git submodule update --init --recursive
script:
  - make travis
